# Secrets — never commit
.env
.env.local

# Dependencies / build output
node_modules/
dist/
out/

# macOS
.DS_Store

# Local agent config (this app is meant to run without an agent)
.claude/settings.local.json

# Source zip — contains a .env with real credentials
# Both names: the old one is kept so a pre-rename zip stays ignored.
rejsud-bot.zip
rejsudai-bot.zip
